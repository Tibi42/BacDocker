# CSRF : login/logout en session (fiable sans JS).
# « submit » reste stateless pour les formulaires Symfony + Turbo.
framework:
    form:
        csrf_protection:
            token_id: submit

    csrf_protection:
        stateless_token_ids:
            - submit
