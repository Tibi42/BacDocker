# Surcharges développement local (chargé automatiquement par Docker Compose).
services:
  php:
    user: root
    environment:
      APP_DEBUG: 1
      XDEBUG_MODE: "off"

  database:
    ports:
      - "3307:3306"
